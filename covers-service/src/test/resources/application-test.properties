spring.application.name=covers-service
security.basic.enabled=false
server.port=8080


# Add some config to use when testing in isolation
cover.service.cover-types=Test Cover
cover.service.random-delay=false